export default function Field({label,type='text',value,onChange,options=[],placeholder=''}){return <label className="field"><span>{label}</span>{type==='textarea'?<textarea value={value??''} onChange={onChange} placeholder={placeholder}/>:type==='select'?<select value={value??''} onChange={onChange}>{options.map(o=>typeof o==='string'?<option key={o} value={o}>{o}</option>:<option key={o.value} value={o.value}>{o.label}</option>)}</select>:<input type={type} value={value??''} onChange={onChange} placeholder={placeholder}/>}</label>}
