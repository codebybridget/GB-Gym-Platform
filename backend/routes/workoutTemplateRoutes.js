import express from 'express';import {protect,authorize} from '../middleware/authMiddleware.js';import {listWorkouts,getWorkout,createWorkout,updateWorkout,deleteWorkout} from '../controllers/workoutTemplateController.js';const r=express.Router();r.get('/',protect,listWorkouts);r.get('/:id',protect,getWorkout);r.post('/',protect,authorize('admin','trainer'),createWorkout);r.put('/:id',protect,authorize('admin','trainer'),updateWorkout);r.delete('/:id',protect,authorize('admin','trainer'),deleteWorkout);export default r
