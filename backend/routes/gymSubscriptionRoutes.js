import express from 'express';import {protect,authorize} from '../middleware/authMiddleware.js';import {getCurrentGymSubscription,getGymSubscriptionHistory,getSaasPlans} from '../controllers/gymSubscriptionController.js';import {initializeGymSubscription} from '../controllers/gymSubscriptionPaymentController.js';
const r=express.Router();r.post('/initialize',protect,authorize('admin'),initializeGymSubscription);r.get('/plans',protect,authorize('admin'),getSaasPlans);r.get('/current',protect,authorize('admin'),getCurrentGymSubscription);r.get('/history',protect,authorize('admin'),getGymSubscriptionHistory);export default r
