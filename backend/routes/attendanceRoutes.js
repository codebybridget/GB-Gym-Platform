import express from 'express';import {protect,authorize} from '../middleware/authMiddleware.js';import {listAttendance,markAttendance,myAttendance} from '../controllers/attendanceController.js';const r=express.Router();r.get('/',protect,authorize('admin','member'),listAttendance);r.get('/my',protect,authorize('member'),myAttendance);r.post('/',protect,authorize('admin'),markAttendance);export default r
